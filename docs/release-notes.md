# MaxSpeedVPN v0.1.23-alpha

## Подписки и выбор сервера

- убрана нижняя плашка параметров активной сессии, которая перекрывала выбор сервера;
- выбранный сервер в списке «Скорость» выделяется мятной рамкой и фоном;
- ручной ввод названия подписки удалён: приложение использует имя провайдера из заголовка `profile-title`, если он передан;
- добавлен безопасный импорт файлов sing-box, а в едином окне — импорт из буфера, файла и QR-кода;
- для файлов поддержаны Naive и Mieru, включая Mieru `server_ports` и диапазоны портов; неподдерживаемые или некорректные конфигурации отклоняются.

## Проверка

- unit tests, Android lint и debug-сборка выполнены;
- целевые UI/API 34 проверки выполнены; диалог импорта и стартовый экран без наложений;
- tag workflow повторяет API 34 smoke Xray и sing-box, собирает и проверяет пять подписанных APK с SHA-256.

> Alpha-сборка использует постоянную release-подпись MaxSpeedVPN.

---

# MaxSpeedVPN v0.1.22-alpha

## Импорт и интерфейс

- поле добавления подписки или сервера поддерживает несколько URI, вставленных построчно, без ослабления проверок ссылок подписок и QR-импорта;
- быстрые свайпы карусели серверов больше не теряют части drag-движения; выбор соседнего сервера остаётся предсказуемым;
- длинные название и package name приложения в split tunneling аккуратно обрезаются и не выталкивают чекбокс;
- после «Поделиться» окно конфигурации сервера закрывается до открытия системного chooser.

## Проверка

- unit tests, Android lint и debug-сборка успешно выполнены;
- импорт и диалог проверены на API 34 эмуляторе; целевая visual QA не выявила наложений или обрезки элементов;
- tag workflow повторяет unit/lint, API 34 smoke для Xray и sing-box, а также собирает пять подписанных APK с SHA-256.

> Alpha-сборка использует постоянную release-подпись MaxSpeedVPN.

---

# MaxSpeedVPN v0.1.21-alpha

## Ребрендинг: Lust → MaxSpeedVPN

- приложение переименовано в MaxSpeedVPN: имя в лаунчере, быстрый тумблер VPN, логи и резервные копии;
- новый логотип — градиентная иконка с буквой M во всех плотностях (включая round-вариант);
- артефакты обновлений и release-ноты переименованы в MaxSpeedVPN-*;
- встроенный обновлятор ищет новые APK по имени MaxSpeedVPN, update smoke приведён к новым именам логов;
- release-подпись и applicationId не менялись — обновление с v0.1.20-alpha проходит штатно.

## Проверка

- unit tests, Android lint и сборка: успешно;
- Xray и sing-box API 34 runtime smoke: пройдены в release workflow;
- пять release APK публикуются с SHA-256 и постоянной release-подписью MaxSpeedVPN.

> Alpha-сборка использует постоянную release-подпись MaxSpeedVPN.

---

# Lust v0.1.20-alpha

## Статистика активной VPN-сессии

- на главную добавлены текущие скорости загрузки и отдачи, общий трафик и длительность соединения;
- карточка показывает ping, VPN-ядро, протокол и безопасный endpoint фактически подключённого сервера;
- трафик берётся из HEV/TUN-счётчиков текущего туннеля и не включает фоновые запросы приложения;
- скорость и длительность рассчитываются по монотонным часам, без скачков системного времени;
- endpoint скрывается при признаках URI, credentials, UUID или Base64-токена, включая точечные варианты с DNS-суффиксом;
- metadata активного сервера сохраняется через выдачу VPN-разрешения и при повторной попытке подключения.

## Проверка

- unit tests, Android lint и сборка: успешно;
- API 34 connected smoke: все поля карточки видимы без обрезки и перекрытий;
- Xray и sing-box runtime smoke выполняются release workflow;
- пять release APK публикуются с SHA-256 и постоянной release-подписью Lust.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.18-alpha

## Карусель и пустое состояние

- карусель серверов напрямую следует за пальцем и поддерживает медленный drag и быстрый fling в обе стороны;
- после отпускания флаги плавно доводятся до соседнего слота без прежней замены всего блока;
- пустая карточка стала большой кнопкой `+ Добавить подписку`, сразу открывающей подписки;
- удалены слова «в настройках» и непонятная обрезанная mint-полоска.

## Проверка

- 104 unit-теста и 4 Android instrumentation-теста;
- непрерывность свайпа подтверждена on-device кадрами во время 2,2-секундного drag;
- RU/EN пустое состояние проверено визуально на API 34;
- Android lint без ошибок;
- Xray и sing-box runtime smoke на API 34: по два цикла TCP/DNS/disconnect, fatal gate без ошибок;
- пять release APK с SHA-256 и постоянной release-подписью Lust.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.17-alpha

## Интерфейс и серверы

- флаг сервера определяется строго из его названия с поддержкой распространённых RU/EN названий, городов и ISO-кодов;
- спидометр получил динамические диапазоны 1/10/100/500/1000 Mbps и больше не округляет низкую скорость до нуля;
- карусель серверов перелистывается плавно и направленно, а имя выбранного сервера меняется без резкого скачка;
- кнопка добавления подписки стала компактной кнопкой `+` с доступной 48 dp областью нажатия.

## Проверка

- импорт реальной подписки: 6 профилей, без пропусков, ошибок и дублей;
- 101 unit-тест и 4 Android instrumentation-теста;
- Android lint без ошибок;
- Xray и sing-box runtime smoke на API 34: по два цикла TCP/DNS/disconnect, fatal gate без ошибок;
- пять release APK с SHA-256 и постоянной release-подписью Lust.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.16-alpha

## Навигация и подписки

- исправлено открытие Telegram-канала на Android 11+;
- добавлены направленные slide/fade-переходы между разделами и плавно перемещающаяся mint-capsule нижней навигации;
- при отсутствии подписок на главной появился переход в единый экран управления подписками;
- экран подписок возвращает пользователя в исходную вкладку и больше не открывает удалённый legacy dashboard;
- добавлены RU/EN подписи и API 34 regression-тесты новых маршрутов.

## Проверка

- 96 unit-тестов и 4 Android instrumentation-теста;
- Android lint без ошибок;
- Xray и sing-box runtime smoke на API 34: по два цикла TCP/DNS/disconnect, fatal gate без ошибок;
- пять release APK с SHA-256 и постоянной release-подписью Lust.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.15-alpha

## Новый интерфейс MaxSpeedVPN

- добавлен безопасный импорт Mieru-серверов по `mieru://` ссылке с экраном подтверждения;

- обновлены главная, экран скорости и настройки в едином тёмном дизайне;
- добавлены RU/EN локализация, фиксированная общая шапка и Telegram-канал;
- навигация использует Iconify regular/filled иконки, плавные переходы и haptic feedback;
- выбранный сервер выделяется размером и мягкой подсветкой без ободка.

## Скорость и серверы

- настоящий тест скорости выполняет download, затем upload, со шкалой 100/500/1000 Mbps;
- массовый ping проверяет до 10 серверов одновременно с timeout 3 секунды;
- long press открывает одиночный ping и просмотр полного конфига с системным Share.

## Проверка

- 96 unit-тестов, Android lint без ошибок и пять release APK;
- API 34 RU/EN: основные вкладки, fixed header, mass ping, long-press popup и download → upload;
- ABI и universal APK проверены по versionName/versionCode, SHA-256 и постоянной release-подписи Lust.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.14-alpha

Проверочный patch-релиз для полного пути обновления из `v0.1.13-alpha`, где уже присутствует исправленный переход к системному разрешению Android.

## Проверка

- 85 unit-тестов, Android lint и сборка пяти APK;
- оба VPN-ядра на Android API 34;
- полный путь `v0.1.13-alpha → v0.1.14-alpha` через GitHub updater и системный Package Installer.

> Alpha-сборка использует постоянную release-подпись Lust.

# Lust v0.1.13-alpha

## Исправление автообновления

- системный экран «Установка неизвестных приложений» теперь корректно открывается из application context;
- сохранены проверки ABI, SHA-256, package/version и release-сертификата перед установкой;
- установка остаётся только системной и требует явного подтверждения пользователя.

## Проверка

- 85 unit-тестов, Android lint и сборка пяти APK;
- оба VPN-ядра на Android API 34;
- реальный путь updater до системного экрана разрешения на Android API 34.

> Alpha-сборка использует постоянную release-подпись Lust.

# Lust v0.1.12-alpha

Проверочный patch-релиз для полного пути встроенного обновления из `v0.1.11-alpha` через GitHub Releases до системного установщика Android. Функциональный состав Mieru и updater не изменён.

## Проверка

- 85 unit-тестов, Android lint и сборка пяти APK;
- оба VPN-ядра на Android API 34;
- загрузка APK по ABI, SHA-256, package/version/certificate pin и системная установка из `v0.1.11-alpha`.

> Alpha-сборка использует постоянную release-подпись Lust.

# Lust v0.1.11-alpha

## Mieru для sing-box

- добавлен импорт официальных ссылок `mierus://` с одиночными портами и диапазонами;
- смешанные TCP/UDP bindings импортируются отдельными профилями без потери параметров;
- поддержан multiplexing; traffic pattern и другие непроверяемые/несовместимые параметры явно отклоняются до запуска ядра;
- нативный Mieru outbound проверен встроенным `sing-box 1.13.14-extended-2.5.2-lust` на Android API 34.

## Обновление приложения

- в настройках добавлена ручная проверка новых prerelease-версий через GitHub Releases;
- приложение выбирает APK под ABI устройства с fallback на universal;
- перед системной установкой проверяются размер, SHA-256, package name, повышение версии и постоянный сертификат Lust;
- тихая установка не используется: Android показывает штатное подтверждение обновления.

## Проверка

- 85 unit-тестов, Android lint и сборка пяти APK;
- реальная проверка GitHub updater и Mieru runtime на Android API 34.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.10-alpha

## Постоянная подпись APK

- release APK теперь подписываются постоянным сертификатом Lust, а не временным debug-ключом GitHub runner;
- следующие версии можно устанавливать поверх этой сборки без удаления приложения и потери настроек;
- все ABI-варианты одного релиза используют одинаковый сертификат;
- signing key хранится вне репозитория и восстанавливается в CI только из GitHub Actions Secrets.

## Важно

- при переходе с `v0.1.9-alpha` или более ранней debug-сборки потребуется один раз удалить старое приложение и установить эту версию;
- начиная с `v0.1.10-alpha`, последующие обновления устанавливаются обычным способом поверх текущей версии.

> Alpha-сборка использует постоянную release-подпись Lust.

---

# Lust v0.1.9-alpha

## Интерфейс v2RayTun

- тема переведена на полностью чёрную нейтральную палитру по референсам v2RayTun;
- удалены голубой акцент и синие/сине-серые оттенки фона, карточек, рамок и текста;
- системные панели Android, основной фон и встроенный журнал используют чистый чёрный цвет;
- поверхности, навигация, элементы управления и настройки приведены к нейтральной серой гамме.

## Проверка

- главный экран и настройки проверены на Android API 34;
- pixel-level анализ подтвердил нейтральные RGB-поверхности без синего цветового смещения.

> Alpha-сборка использует debug-подпись и предназначена для тестирования.

---

# Lust v0.1.8-alpha

## Исправление sing-box

- устранена ложная ошибка `SOCKS 127.0.0.1:10808 is already in use` при повторном подключении;
- проверка порта по-прежнему блокирует реально занятый listener, но допускает корректно освобождённый сокет после остановки sing-box.

## Проверка

- regression test активного и освобождённого локального порта;
- реальные VLESS/XHTTP-профили на удалённых портах `443`, `444` и `8443`;
- по два последовательных цикла подключения и отключения sing-box на Android API 34;
- TCP-трафик через туннель и отсутствие fatal events.

> Alpha-сборка использует debug-подпись и предназначена для тестирования.

---

# Lust v0.1.7-alpha

## Новый главный экран

- экран подключения переделан по присланному тёмному референсу: таймер, центральная power-кнопка, статус и активный сервер;
- подписка, серверы, поиск, проверка задержки и управление профилями собраны в плотный единый экран;
- технический журнал встроен в главную как сворачиваемая секция «ЛОГИ» с очисткой и экспортом;
- нижняя навигация приведена к пунктам «Подключение» и «Настройки» без pill-индикатора;
- таймер показывает фактическую продолжительность активного VPN-сеанса.

## Проверка

- 75 unit-тестов, Android lint и сборка universal + четырёх ABI APK;
- реальная установка x86_64 APK на Android API 34;
- UI Automator проверил главный экран, раскрытие и сворачивание журнала;
- запуск без `FATAL EXCEPTION`.

> Alpha-сборка использует debug-подпись и предназначена для тестирования.

---

# Lust v0.1.6-alpha

## Новые протоколы sing-box

- Hysteria2 и alias `hy2://` с TLS, SNI, ALPN, bandwidth и Salamander obfuscation;
- TUIC с UUID/password, congestion control, UDP relay mode и TLS;
- NaiveProxy через `naive+https://` на Cronet-enabled core;
- VLESS/XHTTP, включая TLS/Reality, uTLS fingerprint, host, path, mode и `xPaddingBytes` из `extra`.

## Ядро

- sing-box заменён на воспроизводимый Android API 26 build `1.13.14-extended-2.5.2-lust.1`;
- exact source: `shtorm-7/sing-box-extended@ff11f007ec798136a5de258f947a4f34011a37ea`;
- все четыре ABI закреплены SHA-256 и распространяются отдельным versioned core prerelease;
- Xray отклоняет sing-box-only профили с понятным сообщением вместо запуска несовместимого JSON.

## Проверка

- 75 unit-тестов;
- Android lint и сборка всех ABI + universal APK;
- `sing-box check` на Android API 34 для Hysteria2, TUIC, Naive и VLESS/XHTTP;
- два connect/disconnect цикла, TCP и DNS smoke отдельно для sing-box и Xray, без fatal logs.

> Alpha-сборка использует debug-подпись и предназначена для тестирования.

---

# Lust v0.1.4-alpha

## Что добавлено

- единая главная с подключением, списком серверов и подписками;
- поиск, группы, избранное и сортировка серверов по имени или TCP endpoint latency;
- безопасный deep-link и clipboard импорт подписок с подтверждением;
- отображение трафика, лимита и срока подписки из `subscription-userinfo`;
- versioned backup через системный Android document picker;
- три понятных режима маршрутизации для Xray и sing-box: всё через VPN, обход LAN и собственные исключения доменов/IP;
- universal APK и отдельные APK для `arm64-v8a`, `armeabi-v7a`, `x86_64`, `x86`;
- SHA-256 checksums для всех APK.

## Что изменено

- нижняя навигация сокращена до «Главная», «Журнал», «Настройки»;
- основной экран настроек упрощён до выбора маршрутизации; технические параметры доступны по кнопке «Расширенные настройки»;
- URL подписки больше не отображается на главной, чтобы не раскрывать access token;
- ABI-specific APK примерно на 65–68% меньше universal debug APK;
- обновлены README, changelog и правила публикации релизов.

## Безопасность

- внешняя ссылка не запускает автоматическую загрузку подписки;
- backup полностью валидируется перед заменой состояния и требует подтверждения;
- содержимое clipboard, backup и subscription URL не записывается в журнал.

## Проверка

Релиз публикуется только после unit/build/lint, Android API 34 smoke для Xray и sing-box, проверки disconnect/reconnect и отсутствия fatal logs.

> Alpha-сборка использует debug-подпись и предназначена для тестирования.
