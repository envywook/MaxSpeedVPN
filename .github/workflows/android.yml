name: Android CI and Release

on:
  push:
    branches: [main, 'release/**']
    tags: ['v*']
    paths-ignore:
      - 'README.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      MAXSPEED_TELEGRAM_URL: https://t.me/ZmaxspeedvpnZ
      GITHUB_TOKEN: ${{ github.token }}

    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Set up Java 17
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a

      - name: Set up Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407

      - name: Install Android SDK packages
        run: sdkmanager 'platforms;android-34' 'build-tools;34.0.0'

      - name: Restore verified native dependency cache
        uses: actions/cache@v4
        with:
          path: .native-build
          key: native-deps-${{ runner.os }}-${{ hashFiles('scripts/prepare-native-deps.sh') }}

      - name: Prepare verified native dependencies
        run: ./scripts/prepare-native-deps.sh

      - name: Restore release signing key
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          # Legacy secret names are retained so the existing release certificate remains usable.
          KEYSTORE_BASE64: ${{ secrets.LUST_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.LUST_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.LUST_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.LUST_KEY_PASSWORD }}
        run: bash scripts/restore-release-signing.sh

      - name: Test, lint and build APK
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            ./gradlew --no-daemon testDebugUnitTest lintDebug assembleRelease --stacktrace
          else
            ./gradlew --no-daemon testDebugUnitTest lintDebug assembleDebug --stacktrace
          fi

      - name: Stage APK variants
        run: |
          set -euo pipefail
          mkdir -p dist
          shopt -s nullglob
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            apks=(app/build/outputs/apk/release/app-*-release.apk)
            suffix=release
          else
            apks=(app/build/outputs/apk/debug/app-*-debug.apk)
            suffix=debug
          fi
          test "${#apks[@]}" -eq 5
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            bash scripts/verify-release-signature.sh "${apks[@]}"
          fi
          safe_ref="${GITHUB_REF_NAME//\//-}"
          for apk in "${apks[@]}"; do
            variant="$(basename "${apk}" | sed -E "s/^app-(.*)-${suffix}\\.apk$/\\1/")"
            cp "${apk}" "dist/MaxSpeedVPN-${safe_ref}-${variant}.apk"
          done
          (cd dist && sha256sum *.apk | sort | tee SHA256SUMS.txt)

  smoke:
    # ubuntu-24 GitHub-hosted runners currently expose no usable /dev/kvm;
    # API 34 emulator smoke needs the hosted 22.04 image's nested KVM support.
    runs-on: ubuntu-22.04
    timeout-minutes: 35
    env:
      GITHUB_TOKEN: ${{ github.token }}
    strategy:
      fail-fast: false
      matrix:
        engine: [XRAY, SING_BOX]

    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Set up Java 17
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a

      - name: Set up Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407

      - name: Install Android SDK packages
        run: sdkmanager 'platforms;android-34' 'build-tools;34.0.0'

      - name: Restore verified native dependency cache
        uses: actions/cache@v4
        with:
          path: .native-build
          key: native-deps-${{ runner.os }}-${{ hashFiles('scripts/prepare-native-deps.sh') }}

      - name: Prepare verified native dependencies
        run: ./scripts/prepare-native-deps.sh

      - name: Build x86_64 APK
        run: ./gradlew --no-daemon assembleDebug --stacktrace

      - name: Enable KVM acceleration
        id: kvm
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules || true
          sudo udevadm trigger --name-match=kvm || true
          if test -r /dev/kvm -a -w /dev/kvm; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "KVM is unavailable; Android emulator will use software acceleration"
          fi

      - name: Run API 34 VPN smoke
        uses: reactivecircus/android-emulator-runner@a421e43855164a8197daf9d8d40fe71c6996bb0d
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          disable-linux-hw-accel: auto
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          script: |
            apk=app/build/outputs/apk/debug/app-x86_64-debug.apk
            bash scripts/seed-android-smoke.sh "${{ matrix.engine }}" "${apk}"
            SKIP_APK_INSTALL=1 EXPECT_ENGINE="${{ matrix.engine }}" bash scripts/android-vpn-smoke.sh "${apk}"

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build, smoke]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Set up Java 17
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a

      - name: Set up Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407

      - name: Install Android SDK packages
        run: sdkmanager 'platforms;android-34' 'build-tools;34.0.0'

      - name: Restore verified native dependency cache
        uses: actions/cache@v4
        with:
          path: .native-build
          key: native-deps-${{ runner.os }}-${{ hashFiles('scripts/prepare-native-deps.sh') }}

      - name: Prepare verified native dependencies
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./scripts/prepare-native-deps.sh

      - name: Restore release signing key
        env:
          KEYSTORE_BASE64: ${{ secrets.LUST_KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.LUST_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.LUST_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.LUST_KEY_PASSWORD }}
        run: bash scripts/restore-release-signing.sh

      - name: Build and stage signed APK variants
        run: |
          set -euo pipefail
          ./gradlew --no-daemon assembleRelease --stacktrace
          mkdir -p dist
          shopt -s nullglob
          apks=(app/build/outputs/apk/release/app-*-release.apk)
          test "${#apks[@]}" -eq 5
          bash scripts/verify-release-signature.sh "${apks[@]}"
          for apk in "${apks[@]}"; do
            variant="$(basename "${apk}" | sed -E 's/^app-(.*)-release\\.apk$/\\1/')"
            cp "${apk}" "dist/MaxSpeedVPN-${GITHUB_REF_NAME}-${variant}.apk"
          done
          (cd dist && sha256sum *.apk | sort | tee SHA256SUMS.txt)

      - name: Publish GitHub pre-release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 - "${GITHUB_REF_NAME}" docs/release-notes.md /tmp/current-release-notes.md <<'PY'
          from pathlib import Path
          import sys

          tag, source, destination = sys.argv[1:]
          lines = Path(source).read_text().splitlines()
          heading = f"# MaxSpeedVPN {tag}"
          try:
              start = lines.index(heading)
          except ValueError:
              raise SystemExit(f"Release notes heading not found: {heading}")
          end = next((index for index in range(start + 1, len(lines)) if lines[index] == "---"), len(lines))
          Path(destination).write_text("\n".join(lines[start:end]).rstrip() + "\n")
          PY
          gh release create "${GITHUB_REF_NAME}" dist/* \
            --repo "${GITHUB_REPOSITORY}" \
            --title "MaxSpeedVPN ${GITHUB_REF_NAME}" \
            --notes-file /tmp/current-release-notes.md \
            --prerelease \
            --verify-tag
